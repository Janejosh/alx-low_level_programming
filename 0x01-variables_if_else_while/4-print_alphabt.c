#include <stdio.h>
/**
 * main - Entry Point
 *
 * Return: 0 (Success)
 */

int main(void)
{
	char b;

	b = 'a';
	while
		(b <= 'z') {
	if
		(b != 'q' && b != 'e') {
			putchar(b);
		}
			b++;
		}
	putchar('\n');
	return (0);
}
